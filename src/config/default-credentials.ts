/**
 * 主管新建销售、管理员新建主管时使用的初始登录密码（须与首次登录强制改密流程一致）。
 * @deprecated 语义上请优先使用 DEFAULT_NEW_USER_PASSWORD；保留 DEFAULT_SALES_PASSWORD 别名以免大范围改名。
 */
export const DEFAULT_NEW_USER_PASSWORD = "fscrm2026";
export const DEFAULT_SALES_PASSWORD = DEFAULT_NEW_USER_PASSWORD;
